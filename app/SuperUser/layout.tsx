import type { Metadata } from "next";
import "/app/globals.css";
import Footer from '@/components/SuperUser/Footer';
import Header from "@/components/SuperUser/Header";
import SideNavBar from "@/components/SuperUser/SideNavBar";


export const metadata: Metadata = {
  title: "Checkias Dashboard",
  description: "Generated by Checkias",
};

export default function RootLayout({
  children,
}: Readonly<{
  children: React.ReactNode;
}>) {
  return (
    <html lang="en">
      <body className="flex">
        {/* Sidebar */}
        <SideNavBar />
        
        {/* Main Content Section (Dynamic Content Area) */}
        <div className="flex-1 flex flex-col">
          {/* Header */}
          <Header />
          
          {/* Main Content */}
          <main className="ml-48 p-6">{children}</main>

          {/* Footer */}
          <Footer />
        </div>
      </body>
    </html>
  );
}
